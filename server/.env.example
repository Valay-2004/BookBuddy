# Database Configuration
DB_USER=your_database_user
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bookbuddy_db
DB_PASSWORD=your_database_password

# Optional: Use DATABASE_URL for platforms like Render/Railway
# DATABASE_URL=postgresql://user:password@host:port/database

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# For Production Deployment (on Render)
# 1. Go to your Render Dashboard -> Environment
# 2. Add the following variables:
#    - NODE_ENV=production
#    - FRONTEND_URL=https://your-frontend-domain.com
#    - DATABASE_URL=your_production_database_url
#    - JWT_SECRET=a_very_long_random_string
#
# Note: You do NOT need a .env file on Render if you set these in the dashboard.
